SECRET.key

# do NOT ignore the media directory
!media
# but everything inside it
media/*
# except the README
!media/README
!media/fixtures

# do NOT ignore the static directory
!static
# but everything inside it
static/*
# except the README
!static/README
